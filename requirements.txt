# Hermes currently runs in deterministic stub mode.
# Add provider dependencies only when the provider is enabled.
openai>=1.0.0
python-dotenv>=0.21.0
pytest>=9.0.0
fastapi>=0.138.0
uvicorn>=0.49.0
Jinja2>=3.1.0
PyYAML>=6.0.0
# Example future options:
# mlx-lm
# requests
